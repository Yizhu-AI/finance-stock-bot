yfinance>=0.2.40
pandas>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
google-genai>=1.0.0
# pandas-ta-classic, not pandas-ta: the original pandas-ta project's PyPI
# releases now require Python>=3.12, and its last Python-3.11-compatible
# release (0.3.14b0) predates numpy>=2.0 support (it imports the removed
# numpy.NaN). pandas-ta-classic is the actively maintained fork — same
# df.ta.* accessor API, supports numpy>=2.0 and Python>=3.10.
pandas-ta-classic>=0.6.52
backtrader>=1.9.78.123
vectorbt>=1.0.0
# vectorbt pulls in numba (JIT) + plotly. Two known installability snags,
# pinned defensively here rather than left to the resolver:
#  - plotly>=6 renames a trace type vectorbt's theme-init code still
#    references, breaking `import vectorbt` outright. Must stay <6.
#  - pip's default resolver can pick a numba/llvmlite pairing with no
#    prebuilt wheel for your platform, forcing a slow/fragile source build.
#    These versions are confirmed to have wheels; if pip still tries to
#    build from source, install with `pip install --only-binary=:all: -r requirements.txt`.
plotly<6
numba==0.62.1
llvmlite==0.45.1

# Dev tooling: pre-commit secret scanning (see .pre-commit-config.yaml).
# Not needed to run the bot itself — only to install/run the git hook.
pre-commit>=4.0.0
detect-secrets>=1.5.0
